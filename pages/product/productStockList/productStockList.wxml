<view class="container">
  <!-- 搜索栏 -->
  <view class="search-bar">
    <view class="search-box">
      <text class="search-icon">🔍</text>
      <input class="search-input" placeholder="搜索商品名称" value="{{keyword}}" bindinput="onSearchInput" confirm-type="search" />
      <text class="search-clear" wx:if="{{keyword}}" bindtap="onClearSearch">✕</text>
    </view>
  </view>

  <!-- 分类标签 -->
  <scroll-view class="category-scroll" scroll-x>
    <view class="category-list">
      <view class="category-tag {{currentCategory === item ? 'active' : ''}}" wx:for="{{categoryOptions}}" wx:key="item" data-cat="{{item}}" bindtap="onCategoryTap">{{item}}</view>
    </view>
  </scroll-view>

  <!-- 列表 -->
  <view class="product-list">
    <view class="product-item {{item.productStatus === 'off_shelf' ? 'off-shelf' : ''}}" wx:for="{{productList}}" wx:key="_id" data-id="{{item._id}}" bindtap="onGoDetail">
      <view class="product-info">
        <view class="product-name-row">
          <text class="product-name {{item.productStatus === 'off_shelf' ? 'off-shelf-name' : ''}}">{{item.name}}</text>
          <text class="product-cat-tag">{{item.category}}</text>
          <text class="status-tag off-shelf-tag" wx:if="{{item.productStatus === 'off_shelf'}}">已下架</text>
          <text class="status-tag low-stock-tag" wx:elif="{{item.stock <= 5 && item.stock >= 0}}">低库存</text>
        </view>
        <view class="product-meta">
          <text class="product-specs" wx:if="{{item.specs && item.specs.length > 0}}">{{item.specs.join('、')}}</text>
          <text class="product-price">¥{{item.price}}/{{item.unit}}</text>
        </view>
        <view class="product-stock-row">
          <text class="product-stock">库存：{{item.stock || 0}}{{item.unit}}</text>
          <text class="product-cost" wx:if="{{item.cost > 0}}">进价：¥{{item.cost}}</text>
        </view>
        <view class="spec-stock-bar" wx:if="{{item.specStock && item.specStock.length > 0}}">
          <text class="spec-stock-bar-item" wx:for="{{item.specStock}}" wx:key="label">{{item.label}}:{{item.stock || 0}}</text>
        </view>
      </view>
      <text class="product-arrow">›</text>
    </view>
    <view class="empty-state" wx:if="{{productList.length === 0 && !loading}}">
      <text class="empty-icon">📦</text>
      <text class="empty-text">暂无商品</text>
      <text class="empty-hint">添加商品后可管理库存和出入库</text>
      <view class="empty-cta" bindtap="onAddProduct">📦 去添加第一个商品 →</view>
    </view>
    <view class="loading-state" wx:if="{{loading}}">
      <text>加载中...</text>
    </view>
  </view>
</view>
